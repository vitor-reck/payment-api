spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/paymentdb
    username: postgres
    password: postgres@123

logging:
  level:
    org:
      hibernate: ERROR

server:
  servlet:
    context-path: /api/v1